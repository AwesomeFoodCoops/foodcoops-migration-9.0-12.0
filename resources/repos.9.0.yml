../AwesomeFoodCoops:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/AwesomeFoodCoops/odoo-production.git
  merges:
    - origin 9.0
  target: origin 9.0
  shell_command_after: ln -s extra_addons ../repositories/extra_addons && ln -s intercoop_addons ../repositories/intercoop_addons && ln -s louve_addons ../repositories/louve_addons
