AwesomeFoodCoops:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/AwesomeFoodCoops/odoo-production.git
  merges:
    - origin 9.0
  target: origin 9.0
  shell_command_after: "ln -sf extra_addons ../extra_addons && ln -sf intercoop_addons ../intercoop_addons && ln -sf louve_addons ../louve_addons"
